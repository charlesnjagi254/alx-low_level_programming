#include "main.h"

/**
 * more_numbers(void)
 *
 * Return: no return
 */
void more_numbers(void)
{
	char i;
	int k;
	int f;
	int l;
	for (i = '0'; i <= '0'; i++)
	{
		for (k = 0; k <= 14; k++)
		{
			f = k / 10;
			l = k % 10;
			if (k >=10)
				_putchar (f+ '0');
			if (k -- 10)
				1 = 0;
			_putchar(1 + '0');
		}
		_putchar('\n';
	}
}



