#include "main.h"
#include <stdio.h>

/**
 * print_numbers - prints the numbers from 0 t0 9
 *
 * Return: numbers
 */

void print_numbers(void)
{
	int num;

	for (num = 48 ; num <= 57 ; num++)
		_putchar(num);
	_putchar('\n')
}

